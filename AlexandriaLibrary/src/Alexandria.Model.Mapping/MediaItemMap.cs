using Telesophy.Alexandria.Model;
using Telesophy.Alexandria.Persistence;

namespace Telesophy.Alexandria.Model.Mapping
{
    //public class MediaItemMap : MapBase<MediaItem>
    //{
    //    public MediaItemMap()
    //        : base()
    //    {
    //        Map(x => x.Album);
    //        Map(x => x.Artist);
    //        Map(x => x.Date);
    //        Map(x => x.Duration).CustomTypeIs(typeof(TimeSpanType));
    //        Map(x => x.Format);
    //        Map(x => x.Number);
    //        Map(x => x.Path).CustomTypeIs(typeof(UriType));
    //        Map(x => x.Source);
    //        Map(x => x.Title);
    //        Map(x => x.Type);
    //    }
    //}
}
